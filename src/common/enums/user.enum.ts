export enum GenderEnum {
    Male = "male",
    Female = "female",
}

export enum ProviderEnum {
    System = "system",
    Google = "google",
}

export enum RoleEnum {
    User = "user",
    Admin = "admin",
}

export enum UserStatusEnum {
    Active = "active",
    Inactive = "inactive",
    Suspended = "suspended",
    Banned = "banned",
}